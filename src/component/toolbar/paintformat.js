import ToggleItem from './toggle-item';

/**
 * 格式刷按钮。
 * @ignore
 * @class
 */
class PaintFormat extends ToggleItem {
  constructor() {
    super('paintformat', undefined, undefined);
  }

  setState() {}
}

export default PaintFormat;
